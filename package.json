{
  "name": "country-fingerprint-api",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API for the EU Stats site (frontend is in /site, deployed to GitHub Pages)",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5"
  }
}
